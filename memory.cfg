MEMORY {
	RAM1:	start = $0000, size = $4000, type = rw;
	ROM1:	start = $8000, size = $8000, type = ro, fill = yes, file = "firmware.bin";
}

SEGMENTS {
	CODE:		load = ROM1, type = ro;
	VECTORS:	load = ROM1, type = ro, start = $fffa;
}
